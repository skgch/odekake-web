export default class VisitedPlaceDetails {
  constructor (place, date, stayingTime, comments) {
    this.place = place
    this.date = date
    this.stayingTime = stayingTime
    this.comments = comments
  }
}
