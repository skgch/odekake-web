export default class Trip {
  constructor (title, country, city, fromDate, toDate) {
    this.title = title
    this.country = country
    this.city = city
    this.fromDate = fromDate
    this.toDate = toDate
  }
}
